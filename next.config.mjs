// next.config.js

module.exports = {
    basePath: '/repository', // Replace with your GitHub repository name
    assetPrefix: '/repository/', // Replace with your GitHub repository name
    trailingSlash: true,
    // Add your other Next.js configurations here
    // Example:
    // webpack: (config, { isServer }) => {
    //   if (!isServer) {
    //     // Modify webpack config here for client-side
    //   }
    //   return config;
    // },
  };
  